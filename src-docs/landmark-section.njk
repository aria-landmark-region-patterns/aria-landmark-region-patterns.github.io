
                  <ul>
                    <li>The <code>section</code> element is a <code>region</code> landmark when it has a <a href="index.html#aria_lh_step3">label</a>.</li>
                    <li>When possible, use a <code>aria-labelledby</code> reference to a visible heading element (e.g. an <code>h1-h6</code>) for the label.</li>
                </ul>

